h:36020 SharedWebGLContext:single-WebGL2-ctx-for-multi-pane,bypasses-Chrome-16-ctx-limit. API:constructor({fontSize,fontFamily,theme})/init()/getCanvas()/addTerminal(id,grid,cursor)/setViewport(id,x,y,w,h)/removeTerminal(id)/startRenderLoop()/stopRenderLoop()/syncCanvasSize(w,h)/setTheme(partial)/dispose(). Batched rendering:all-pane-instances-in-one-buffer,gl.scissor-per-pane. init():detects-software-renderer(WEBGL_debug_renderer_info+/swiftshader|llvmpipe|software/i),throws-error-on-match,TerminalPane-falls-back-to-per-pane-Canvas2D.
