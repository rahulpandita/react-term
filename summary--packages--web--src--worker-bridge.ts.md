h:8031 WorkerBridge:parser-worker-lifecycle,start/write/resize/dispose. getSharedBuffers()→{sharedBuffer,sharedAltBuffer} sends both SABs in init+resize. HIGH_WATERMARK=2MB,LOW_WATERMARK=512KB,writeQueue,pendingBytes. FlushMessage→onFlush(isAlternate). non-SAB:applyFlush transfers cellData+dirtyRows. updateGrid(grid,altGrid,cursor) for resize.